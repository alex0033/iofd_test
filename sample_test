exec_file="$(dirname $0)/sample.rb"
input_file="$(dirname $0)/input_file"

case1=$(
    ruby $exec_file < $input_file | grep "a+b: 9"
)

if [ -z "$case1" ]
then
    echo "fail case1"
else
    echo "success case1"
fi